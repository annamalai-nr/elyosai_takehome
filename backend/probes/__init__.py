"""Standalone probes for /weather, /research, and their cancellation behaviour.

Each script writes an HTML report to `api_analysis/_archive/`. The
consolidated quirk dossiers live in `api_analysis/weather.html` and
`api_analysis/research.html`; the files here are the raw probing
artefacts those dossiers were synthesised from.
"""
